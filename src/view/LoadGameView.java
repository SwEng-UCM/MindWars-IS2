package view;

import controller.NavigationController;

/** Placeholder load-game screen. Full save/load comes with the Memento work. */
public class LoadGameView extends PlaceholderView {
    public LoadGameView(NavigationController nav) {
        super("Load Game", "(coming soon)", nav);
    }
}
